---
name: api-contracts
description: 'Shared data contracts between backend and frontend: Pydantic models, TypeScript types, OpenAPI components, and validation schemas. Trigger: When defining DTOs, request/response types, or shared interfaces between API and UI.'
version: 1.0
metadata:
  phase:
    - construction
  layer:
    - backend
    - frontend
  enforcement: mandatory
  depends_on:
    - api-first-spec
  consumed_by:
    - api-first-backend
    - api-first-frontend
    - bun-backend
  agent_roles:
    - design-agent
    - delivery-agent
  validation_profile: architecture
  mcp_usage: context7
---

## Purpose

Ensure backend DTOs (Pydantic) and frontend TypeScript types stay aligned by deriving both from a single source of truth, typically the OpenAPI spec generated by `api-first-spec`.

## When to use this skill

Activate when:
- Defining request/response DTOs for a new module
- Generating TypeScript types from OpenAPI schemas
- Need shared validation schemas (Zod) on both sides

## Relation to other skills

| Skill | Relation | Description |
|-------|----------|-------------|
| `api-first-spec` | input | OpenAPI schemas |
| `api-first-backend` | consumer | Pydantic models |
| `api-first-frontend` | consumer | TypeScript types |
| `bun-backend` | consumer | Zod schemas |

## Critical Rules

1. The OpenAPI spec is the single source of truth for contracts.
2. Generate Pydantic models with `datamodel-code-generator`.
3. Generate TypeScript types with `openapi-typescript`.
4. Do not hand-write types that duplicate generated contracts.
5. Version contracts with the API version.

## Response Envelope Contract (canónico)

El formato de respuesta (éxito y error) es único en el framework y está definido en
`error-handling` ("Contrato de Respuestas API"). Resumen para contratos y DTOs:

- **Éxito (2xx):** `{ success: true, data: T, message: string | null }`
- **Error (4xx/5xx, o 2xx con success:false):** `{ success: false, error: { code, message, details? }, data: null, meta: { trace_id, timestamp } }`

Reglas para tipos generados:

1. `data` es el DTO del recurso (`T`); los tipos generados describen solo `T` (el cliente
   generado por `api.ts.j2`/`service.ts.j2` desenvuelve `data` y lanza `ApiError`).
2. Los errores de validación de campos van en `error.details` como array
   `{ code, field, message }`.
3. **Nunca** tratar `HTTP 2xx + success:false` como éxito en el cliente (ver `error-handling`).

## Outputs produced

| Artifact | Path | Description |
|----------|------|-------------|
| Pydantic models | `backend/app/modules/{module}/schemas.py` | Generated from OpenAPI |
| TypeScript types | `frontend/src/app/modules/{module}/models/{module}.model.ts` | Generated from OpenAPI |
| Zod schemas | `frontend/src/app/modules/{module}/models/{module}.schema.ts` | Client validation |

## Example: Generation commands

```bash
# Python
 datamodel-codegen --input docs/api/{module}/openapi.yaml --output backend/app/modules/{module}/schemas.py

# TypeScript
npx openapi-typescript docs/api/{module}/openapi.yaml -o frontend/src/app/modules/{module}/models/{module}.types.ts
```

## Checklist

- [ ] OpenAPI spec is source of truth
- [ ] Generation scripts committed
- [ ] Hand-written types avoided
- [ ] Contracts versioned with API
